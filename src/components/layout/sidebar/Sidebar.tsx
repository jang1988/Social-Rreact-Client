import React from 'react';
import UserItems from './UserItems';

const Sidebar: React.FC = () => {
    return (
        <div>
            <UserItems />
        </div>
    );
};

export default Sidebar;
